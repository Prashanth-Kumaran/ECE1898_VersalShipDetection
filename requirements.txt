# YOLOv5 Ship Detection — Python 3.12 + CUDA 12.7
# ──────────────────────────────────────────────
# DO NOT install via VS Code's "Install from requirements.txt" button.
# Instead run this in the terminal with the venv activated:
#
#   pip install -r requirements.txt
#
# The --extra-index-url line below tells pip where to find CUDA builds.

--extra-index-url https://download.pytorch.org/whl/cu124

# Core deep learning (CUDA 12.4 builds work with CUDA 12.7 drivers)
torch==2.5.1+cu124
torchvision==0.20.1+cu124

# Image processing
opencv-python==4.10.0.84
albumentations==1.4.10

# Utilities
numpy==1.26.4
tqdm==4.66.4
scikit-learn==1.5.1
matplotlib==3.9.1
Pillow==10.4.0