# YOLOv5 Ship Detection — Python 3.11 + CUDA 12.1
# ──────────────────────────────────────────────
# Install with:
#   pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu121
#
# If you have a different CUDA version, change cu121 to:
#   cu118  → CUDA 11.8
#   cu124  → CUDA 12.4
#   cpu    → CPU only (no GPU)

# Core deep learning
torch==2.3.1+cu124
torchvision==0.18.1+cu124

# Image processing
opencv-python==4.10.0.84
albumentations==1.4.10

# Utilities
numpy==1.26.4
tqdm==4.66.4
scikit-learn==1.5.1
matplotlib==3.9.1
Pillow==10.4.0